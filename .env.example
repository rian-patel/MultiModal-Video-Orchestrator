# Copy to .env and fill in. Not needed for Phase 0 (mock pipeline).
ANTHROPIC_API_KEY=
HIGGSFIELD_API_KEY=
# Optional videogen tuning (v2 /v1/image2video/dop endpoint):
# HIGGSFIELD_MODEL=dop-turbo            # dop-turbo | dop-lite | dop-preview
# HIGGSFIELD_MOTION_STRENGTH=0.3        # 0..1; lower = less camera travel = higher fidelity
